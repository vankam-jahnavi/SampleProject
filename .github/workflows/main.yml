name: Reusable Workflow To Deploy MuleSoft Application

on:

  push:

    branches:

      - main
 
jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout this repo

        uses: actions/checkout@v4
 
      - name: Cache dependencies

        uses: actions/cache@v4

        with:

          path: ~/.m2/repository

          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}

          restore-keys: ${{ runner.os }}-maven-
 
      - name: Set up JDK 17

        uses: actions/setup-java@v4

        with:

          distribution: "zulu"

          java-version: 17
 
      # - name: Build JAR

      #   run: mvn clean package -DmuleDeploy
 
      - name: Publish to Exchange

        run: |

          mvn deploy --settings .maven/settings.xml -DskipMunitTests \

          -Danypoint.username="${{ secrets.USER }}" \

          -Danypoint.password="${{ secrets.PASS }}"
 
 
  deploydev:

    needs: build  

    runs-on: ubuntu-latest

    environment: dev

    steps:

      - name: Checkout this repo

        uses: actions/checkout@v4

      - name: Cache dependencies

        uses: actions/cache@v4

        with:

          path: ~/.m2/repository

          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}

          restore-keys: |

            ${{ runner.os }}-maven-

      - name: Deploy to dev

        run: |

          mvn deploy --settings .settings/settings.xml -DskipMunitTests -DmuleDeploy \

          -Danypoint.username="c60aada09ed34f2598dae48f26082b78" \

          -Danypoint.password="Eccc1Ddf6d02467495879aCA4f75D2DB" \

          -Danypoint.businessGroup="469e9fdb-66a0-442a-bd41-668b21a64f7c" \

          -Denv="Dev" \

          -Dappname="order-details" \

          -Danypoint.platform.client_id="606e9450c63b44b78170dda1735ff4e4" \

          -Danypoint.platform.client_secret="0126EA63B5d5490C8Ed8F0ac561FeFCE" \

